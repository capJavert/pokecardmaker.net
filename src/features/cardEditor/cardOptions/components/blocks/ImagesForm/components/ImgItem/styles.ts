import { Typography, styled } from '@mui/material';

export const SrcLabel = styled(Typography)`
  display: block;
  text-overflow: ellipsis;
  overflow: hidden;
  text-align: left;
  white-space: nowrap;
  width: 100%;
`;
